
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all public read/write access to all collections.
    // Your server-side code, running with admin credentials, will still have full access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
